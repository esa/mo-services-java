/**
 * This package contains a simple demonstration application of a service provider.
 * The provider defines a simple parameter like interface and publishes them 
 * using a PubSub pattern. It allows control of the size of the parameter pool,
 * the update block size, and the generation rate.
 */
package esa.mo.mal.demo.provider;
