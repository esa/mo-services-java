<?xml version="1.0" encoding="UTF-8"?>

<!--
    Licensed under the European Space Agency Public License, Version 2.0
    You may not use this file except in compliance with the License.

    Except as expressly set forth in this License, the Software is provided to
    You on an "as is" basis and without warranties of any kind, including without
    limitation merchantability, fitness for a particular purpose, absence of
    defects or errors, accuracy or non-infringement of intellectual property rights.

    See the License for the specific language governing permissions and limitations under the License.
-->

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>int.esa.ccsds.mo</groupId>
        <artifactId>parent</artifactId>
        <version>15.0-SNAPSHOT</version>
        <relativePath>../../parent/pom.xml</relativePath>
    </parent>

    <artifactId>api-generator-lib</artifactId>
    <version>15.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>ESA MO API Generator - Library</name>
    <description>Model of MO service specifications, with importers, exporters and generators</description>

    <organization>
        <name>ESA</name>
        <url>http://www.esa.int</url>
    </organization>

    <licenses>
        <license>
            <name>The European Space Agency Public License, Version 2.0</name>
            <url>https://raw.githubusercontent.com/esa/mo-services-java/master/LICENCE.md</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <scm>
        <connection>scm:git:git@github.com:esa/mo-services-java.git</connection>
        <developerConnection>scm:git:git@github.com:esa/mo-services-java.git</developerConnection>
        <url>https://github.com/esa/mo-services-java</url>
    </scm>

    <issueManagement>
        <system>GitHub</system>
        <url>https://github.com/esa/mo-services-java/issues</url>
    </issueManagement>

    <properties>
        <!--
            The schemas are unpacked into the library's resources so that exported XML can
            be validated against the normative definition (design section 6.1). Nothing is
            code-generated from them.
        -->
        <ccsds.specification.download.skip>false</ccsds.specification.download.skip>
        <ccsds.specification.download.artifact>xml-ccsds-mo-standards</ccsds.specification.download.artifact>
        <ccsds.specification.download.directory>${project.build.outputDirectory}</ccsds.specification.download.directory>
        <ccsds.specification.download.filter>**/*.xsd</ccsds.specification.download.filter>
    </properties>

    <!--
        No third-party dependencies. The model is plain Java, XML I/O uses StAX from the
        JDK, the MOSpec parser is hand-written, and docx and XHTML output are written as
        text. Batik was here to rasterise diagrams declared inside a specification; that
        support is gone, and with it the last third party. See DESIGN.md, sections 3
        and 8.3.

        The heavy tests - the golden trees, the corpus walks and the round trips - live in
        testbeds/testbed-api-generator, not here, so that building this module stays a
        compile. See DESIGN.md, section 10.8.
    -->
    <dependencies>
        <!-- Build-time only: supplies the XSDs unpacked above, and orders the reactor. -->
        <dependency>
            <groupId>int.esa.ccsds.mo</groupId>
            <artifactId>xml-ccsds-mo-standards</artifactId>
            <version>15.0-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <resource>
                <directory>${basedir}/..</directory>
                <includes>
                    <include>LICENCE.md</include>
                </includes>
            </resource>
            <!-- Naming any resource directory replaces the default, so the module's own
                 has to be named again: it holds the fixed parts of a .docx package. -->
            <resource>
                <directory>src/main/resources</directory>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
